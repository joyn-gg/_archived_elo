using System;
using System.Collections.Generic;
using System.Text;

namespace ELO.Entities
{
    public enum LeaderboardSortMode
    {
        point = 10,
        points = 10,
        wins = 20,
        win = 20,
        losses = 30,
        lose = 30,
        loss = 30,
        wlr = 40,
        winloss = 40,
        games = 50,
        gamesPlayed = 50
    }
}
